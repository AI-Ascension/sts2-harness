# Consumed `poc-v1` artifact

This is the offline release-like artifact copy consumed by the deterministic harness runner. The
harness verifies its identity and fixture lineage locally; it does not import protocol
implementation modules or link to a sibling repository.
