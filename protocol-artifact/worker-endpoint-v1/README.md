# Watchdog-owned endpoint conformance data

Owner/source: AI-Ascension/ascension-watchdog,
`schemas/worker-endpoint-v1/conformance.json`. License: MIT.
Imported as an exact data artifact; no watchdog implementation code is imported.

SHA-256: `2f7d9758a681ed77d633e3ed576de1a415734b7ef96255f099e0cb6c9b658f41`.
The synthetic values contain no credentials, private paths, or game material.
The Linux harness test independently decodes bootstrap nonces and resolves the
Linux cases. Windows cases are retained byte-for-byte but do not imply a Windows
listener. Updates require reviewed owner contract data and an explicit digest
change in the consumer test. Exact owner publication revision is tracked with
the integration evidence, not inferred from matching interface names.
