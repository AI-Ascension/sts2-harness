# Mermaid sources

`global-architecture.mmd` and `decision-policy.mmd` describe the common fair-play and transactional
workflow. `states/` contains one generated source per candidate state. These files are conservative
source diagrams, not visual validation evidence. Render them with a pinned Mermaid tool only after
the target-build validation campaign defines the signatures and transitions they should depict.
